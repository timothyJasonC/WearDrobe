{
  "buildCommand": "cd apps/api && npx prisma generate && cd ../.. && npm run build",
  "outputDirectory": "/.next",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    },
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}